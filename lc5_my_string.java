class Solution {
    public String longestPalindrome(String s) {
        List<Integer> l1 = new LinkedList<>();
        for(int i =0;i<s.length()-1;i++){
            if(s.charAt(i)==s.charAt(i+1)) l1.add(i++);
        }
        int max = (l1.size()==)
        while(true){
            int len1 = l1.size();
            for(int i = 0;i<len;i++){
                if(i>0&&i<s.length()-2&&s.charAt(i-1)==s.charAt(i+2)) a.set(i,l1.get(i--));
                else re
            }
        }
    }
}